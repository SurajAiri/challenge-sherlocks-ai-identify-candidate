{
  "name": "engine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn main:api --reload --host 0.0.0.0 --port 8090 --env-file .env",
    "build": "echo 'No build step required'",
    "start": "uv run uvicorn main:api --host 0.0.0.0 --port 8090"
  }
}